A. 핵심 개요:

LLM 채팅창 서비스는 크게 3가지 파트로 구성
Extension 서버: 챗봇 화면(HTML/JS) 파일을 서빙만 하는 정적 파일 서버. 로직 없음.
백엔드 서버: 질문을 받아서 Gemini에게 판단시키고, 필요하면 tableau-mcp에 데이터 조회를 시키는 지휘자 역할. 인증/권한 처리도 여기서 함.
tableau-mcp: Tableau 공식 오픈소스. 실제로 Tableau Server/Cloud와 통신해서 데이터를 조회해오는 역할 (RLS도 여기서 적용됨).




Tableau Server나 Cloud에 로그인하는 방식은 "하나의 서비스 계정"을 사용하는 방식 채용


-------------------------------------------------------------------------------

B. 프로세스:
1. Tableau Server/Cloud에 로그인 → 게시된 대시보드 열람 사용자가 Tableau 자체 로그인 화면으로 로그인하고, 챗봇이 들어있는 대시보드를 엶

2. Extension 코드 로드 브라우저가 Extension 서버(8765)에 코드를 요청

3. 코드 요청으로, HTML/JS를 받아와서 화면을 그림

4. 대시보드 컨텍스트 자동 수집 질문하기 전에 미리, 현재 보고 있는 워크시트명/데이터소스명을 자동으로 모아둠

5. 권한(허용 지역) 확보 — 이 방식의 핵심 Extension이 대시보드 안에 숨겨진 AllowedRegions 워크시트를 읽습니다. 이 워크시트는 RLS(ISMEMBEROF)가 이미 걸려있어서, 지금 로그인한 사용자가 실제로 볼 수 있는 지역만 담겨서 내려옴

6. 질문 전송 사용자가 질문을 입력하면, Extension이 질문 + 4번의 컨텍스트 + 5번의 allowedRegions를 백엔드(POST /query/group-rls)로 보냄

7. 백엔드 인증 (고정 서비스 계정) 백엔드가 캐시된 토큰이 있으면 그걸 재사용하고, 없거나 3시간 넘게 오래됐으면 그때만 Tableau REST API(POST /auth/signin)로 서비스 계정 아이디+비밀번호로 로그인. Tableau Server가 세션 토큰을 발급해주면, 백엔드가 그걸 자체적으로 캐싱.

반복 시작 (최대 12회) — Gemini와 tableau-mcp 사이 왕복

8. 백엔드 → Gemini: 질문 + 컨텍스트 + 프롬프트 전달 (2회차부터는 이전 도구 호출 이력도 포함)

9. Gemini 판단: 이미 답할 수 있으면 → 15번으로 바로 이동 / 도구 호출이 더 필요하면 → 다음 단계

10. 백엔드 → tableau-mcp: 도구 호출 내용 전달. 이때 도구가 query-datasource라면, 백엔드가 Gemini의 요청에 allowedRegions 기반 필터를 코드로 강제로 끼워 넣음 (LLM이 빼거나 바꿀 수 없음). 다른 도구(list-datasources 등)는 그냥 그대로 전달.

11. tableau-mcp → Tableau Server: 요청 전달

목록/메타데이터 조회 → REST API, 필터와 무관
실제 데이터 조회(query-datasource) → VizQL Data Service, 이번엔 Tableau 자체 RLS가 아니라 10번에서 주입된 강제 필터가 실질적인 제한 역할을 함. (서비스 계정 자체는 전체 데이터를 볼 수 있지만, 필터 때문에 허용된 지역만 반환됨)

12. Tableau Server → tableau-mcp → 백엔드 → Gemini: 데이터 전달

13. Gemini 재판단: 답이 부족하면 → 8번부터 재시작 / 충분하면 → 다음 단계

반복 종료

14. 최종 답변 전달 백엔드가 최종 답변을 브라우저(Extension 화면)에게 전달

15. 화면 표시 브라우저가 마크다운을 안전하게 HTML로 변환해서 채팅창에 답변을 출력